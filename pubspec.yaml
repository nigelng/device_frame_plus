name: device_frame_plus
description: Mockups for common devices.
version: 1.3.0
issue_tracker: https://github.com/StorybookToolkit/device_frame_plus/issues
repository: https://github.com/StorybookToolkit/device_frame_plus

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.2.0'

dependencies:
  freezed_annotation: ^3.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.4
  json_serializable: ^6.0.1
  freezed: ^3.0.3
  flutter_lints: ^5.0.0
